<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@android:color/transparent">

    <!-- DATE HEADER -->
    <TextView
            android:id="@+id/tvDateHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/spacing_xxs"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:text="@string/today"
            android:textSize="10sp"
            android:textColor="@color/text_tertiary"
            android:textStyle="bold"
            android:letterSpacing="0.15"
            android:textAllCaps="true"
            android:visibility="gone"
            tools:visibility="visible"/>

    <!-- CHAT SESSION CARD WITH BORDER -->
    <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/spacing_xs"
            app:cardBackgroundColor="@color/surface"
            app:cardElevation="0dp"
            app:strokeWidth="1dp"
            app:strokeColor="@color/border_light"
            app:cardCornerRadius="@dimen/radius_md">

        <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/itemContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackground"
                android:padding="@dimen/spacing_md">

            <!-- AI Avatar with Loading Ring -->
            <FrameLayout
                    android:id="@+id/avatarContainer"
                    android:layout_width="45dp"
                    android:layout_height="45dp"
                    app:layout_constraintStart_toStartOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    app:layout_constraintBottom_toBottomOf="parent">

                <!-- Background Circle -->
                <View
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="@drawable/bg_icon_teal"/>

                <!-- Loading Ring (Shows when AI is thinking) -->
                <ImageView
                        android:id="@+id/ivLoadingRing"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:padding="-2dp"
                        android:src="@drawable/bg_gemini_ring_gradient"
                        android:scaleType="fitXY"
                        android:visibility="gone"
                        tools:visibility="visible"/>

                <!-- AI Icon -->
                <ImageView
                        android:id="@+id/ivAiIcon"
                        android:layout_width="@dimen/icon_lg"
                        android:layout_height="@dimen/icon_lg"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_gemini_sparkle"
                        android:padding="4dp"
                        android:contentDescription="@string/my_garden"/>

            </FrameLayout>

            <!-- INFO SECTION -->
            <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/spacing_md"
                    android:layout_marginEnd="@dimen/spacing_xs"
                    android:orientation="vertical"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toStartOf="@+id/tvTime"
                    app:layout_constraintStart_toEndOf="@+id/avatarContainer"
                    app:layout_constraintTop_toTopOf="parent">

                <!-- Chat Title -->
                <TextView
                        android:id="@+id/tvTitle"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Hỏi về cây hoa hồng"
                        android:textSize="16sp"
                        android:textColor="@color/text_primary"
                        android:textStyle="bold"
                        android:fontFamily="sans-serif-medium"
                        android:maxLines="1"
                        android:ellipsize="end"
                        tools:text="Hỏi về cây hoa hồng"/>

                <!-- Last Message Preview -->
                <TextView
                        android:id="@+id/tvLastMessage"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="2dp"
                        android:text="Nội dung tin nhắn cuối..."
                        android:textSize="13sp"
                        android:textColor="@color/text_secondary"
                        android:maxLines="1"
                        android:ellipsize="end"
                        tools:text="Hoa hồng là loại thực vật thuộc họ..."/>

            </LinearLayout>

            <!-- Time Stamp -->
            <TextView
                    android:id="@+id/tvTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="14:30"
                    android:textSize="12sp"
                    android:textColor="@color/text_tertiary"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="parent"
                    app:layout_constraintTop_toTopOf="parent"
                    tools:text="14:30"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>

</LinearLayout>