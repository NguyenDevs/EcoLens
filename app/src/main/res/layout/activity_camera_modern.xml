<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:animateLayoutChanges="true"
        android:layout_height="match_parent"
        android:background="@color/black"
        tools:context=".activities.CameraActivity">

    <!-- MODERN CAMERA ACTIVITY -->

    <!-- CAMERA PREVIEW -->
    <androidx.camera.view.PreviewView
            android:id="@+id/viewFinder"
            android:layout_width="match_parent"
            android:layout_height="550dp"
            android:layout_marginTop="70dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toTopOf="@+id/captureButton"/>

    <!-- FOCUS INDICATOR -->
    <ImageView
            android:id="@+id/focusIndicator"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:src="@drawable/bg_focus_ring"
            android:visibility="invisible"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:ignore="ContentDescription"/>

    <!-- TOP BAR CONTROLS -->
    <ImageView
            android:id="@+id/flashToggle"
            android:layout_width="@dimen/icon_xl"
            android:layout_height="@dimen/icon_xl"
            android:layout_marginTop="50dp"
            android:layout_marginStart="@dimen/spacing_lg"
            android:src="@drawable/ic_lightning_off"
            android:contentDescription="@string/flash_toggle"
            app:tint="@color/white"
            android:padding="@dimen/spacing_xs"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

    <ImageView
            android:id="@+id/closeButton"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginEnd="@dimen/spacing_lg"
            android:layout_marginTop="48dp"
            android:src="@drawable/ic_close"
            android:contentDescription="@string/close"
            app:tint="@color/white"
            android:padding="@dimen/spacing_xs"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    <!-- VIEWFINDER FRAME -->
    <FrameLayout
            android:id="@+id/viewfinderFrame"
            android:layout_width="350dp"
            android:layout_height="0dp"
            android:foreground="@drawable/camera_corner"
            app:layout_constraintTop_toTopOf="@+id/viewFinder"
            app:layout_constraintStart_toStartOf="@+id/viewFinder"
            app:layout_constraintEnd_toEndOf="@+id/viewFinder"
            app:layout_constraintBottom_toBottomOf="@+id/viewFinder"
            app:layout_constraintDimensionRatio="3:3"/>

    <!-- BOTTOM CONTROLS -->
    <ImageView
            android:id="@+id/uploadButton"
            android:layout_width="@dimen/fab_size_normal"
            android:layout_height="@dimen/fab_size_normal"
            android:src="@drawable/ic_upload"
            android:contentDescription="@string/upload"
            app:tint="@color/white"
            android:padding="14dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:layout_marginBottom="40dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/captureButton"
            app:layout_constraintStart_toStartOf="parent"/>

    <ImageView
            android:id="@+id/captureBorder"
            android:layout_width="68dp"
            android:layout_height="68dp"
            android:src="@drawable/capture_border_thin"
            android:scaleType="fitXY"
            android:clickable="false"
            android:focusable="false"
            android:contentDescription="@string/capture"
            android:layout_marginBottom="37dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    <ImageView
            android:id="@+id/captureButton"
            android:layout_width="58dp"
            android:layout_height="58dp"
            android:background="@drawable/bg_circle_white"
            android:src="@drawable/ic_search"
            app:tint="@color/primary_dark"
            android:contentDescription="@string/capture"
            app:elevation="@dimen/elevation_xs"
            app:layout_constraintTop_toTopOf="@+id/captureBorder"
            app:layout_constraintBottom_toBottomOf="@+id/captureBorder"
            app:layout_constraintStart_toStartOf="@+id/captureBorder"
            app:layout_constraintEnd_toEndOf="@+id/captureBorder"
            android:paddingStart="12dp" android:paddingEnd="10dp" android:paddingTop="3dp"/>
    <ImageView
            android:id="@+id/refreshButton"
            android:layout_width="@dimen/fab_size_normal"
            android:layout_height="@dimen/fab_size_normal"
            android:src="@drawable/ic_rotate"
            android:contentDescription="@string/rotate"
            app:tint="@color/white"
            android:padding="14dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:layout_marginBottom="40dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/captureButton"
            app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>